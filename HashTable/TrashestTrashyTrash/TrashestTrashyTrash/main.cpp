#include <iostream>
#include <iterator>
#include <vector>
int main() {
    
    
    std::string a("12345");
    std::string b("qwerty");
    std::string c;
//    a.insert(a.begin() + 1, b.begin() + 1, b.end());
//    a.erase(a.begin(), a.end() - 1);
    std::vector<std::string> lines;
    
}